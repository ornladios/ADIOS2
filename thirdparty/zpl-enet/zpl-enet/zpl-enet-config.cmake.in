include(FindPackageHandleStandardArgs)
set(${CMAKE_FIND_PACKAGE_NAME}_CONFIG "${CMAKE_CURRENT_LIST_FILE}")
find_package_handle_standard_args(${CMAKE_FIND_PACKAGE_NAME} CONFIG_MODE)


if(NOT TARGET zpl-enet::zpl-enet)
#  include("${CMAKE_CURRENT_LIST_DIR}/zpl-enet-targets.cmake")
endif()

set(ENET_LIBRARIES zpl-enet::zpl-enet)
set(ENET_INCLUDE_DIRS
  $<TARGET_PROPERTY:zpl-enet::zpl-enet,INTERFACE_INCLUDE_DIRECTORIES>
)
