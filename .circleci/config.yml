version: 2
jobs:
  build:
    working_directory: /home/adios2/hidecirclesource
    docker:
      - image: ornladios/adios2:el7-ohpc
    parallelism: 2
    steps:
      - checkout
      - run:
        name: Run dashboard scripts
        type: shell
        command: |
          circleci tests glob "scripts/dashboard/circle_*.cmake" | circleci tests split | xargs bash scripts/circle/runOnCircle.sh
